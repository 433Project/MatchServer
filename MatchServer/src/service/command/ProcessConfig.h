#pragma once
#include "Protocol.h"
#include "Packet_generated.h"

using namespace std;

class ProcessConfig
{
public:
	ProcessConfig();
	~ProcessConfig();
	//void Process(SOCKET s, Header* h, Body* b);
};

